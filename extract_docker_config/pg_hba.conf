# Allow all IPv4 connections with md5 password auth
host    all             all             0.0.0.0/0               md5

# Local Unix socket access (inside container)
local   all             all                                     peer
